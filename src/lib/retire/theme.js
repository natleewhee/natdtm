// src/lib/retire/theme.js
// RetireWell uses the shared design tokens and money helpers verbatim —
// see src/lib/shared/theme.js. Re-exported so existing
// `@/lib/retire/theme` imports keep working unchanged.
export { C, SGD, parseMoney } from '../shared/theme.js'
