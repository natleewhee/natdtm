# LTA DataMall AccountKey, used by src/app/drive/api/coe/route.js to fetch
# live COE premiums. Request one at https://datamall.lta.gov.sg/.
# Without this set, /drive falls back to the hardcoded COE_FALLBACK
# constants in src/lib/drive/calc.js.
LTA_API_KEY=
